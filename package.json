{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "HOST=0.0.0.0 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/multer": "^1.4.7",
    "@types/node": "^18",
    "nuxt": "^3.4.2"
  },
  "dependencies": {
    "multer": "*"
  }
}
